# config.env
# ==========
# Shared configuration — committed to the repo.
# Contains org and project settings that are the same for everyone on the team.
# Does NOT contain any secrets or tokens.

ADO_ORG=gameballers
ADO_PROJECT=Support-QA
